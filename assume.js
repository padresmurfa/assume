import Assume from 'src/assume';
import AssumptionFailed from 'src/assumptionFailed';
import normalizeClassNames from 'src/normalizeClassNames';
import singleton from 'src/singleton';

export default singleton;

export {
    AssumptionFailed,
    Assume,
    normalizeClassNames
};
